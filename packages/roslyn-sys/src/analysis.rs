//! Reserved for future Rust-side Roslyn helpers (e.g. cached symbol
//! tables). Currently empty: all Roslyn interaction happens through the
//! managed helper, and the bridge returns JSON that the analyzer
//! converts into domain types directly.

#![allow(dead_code)]
